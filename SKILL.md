---
name: security-attendance-payroll
description: 核对安保人员原始打卡记录，按班组规则生成严格保留模板格式的月度考勤表，并结合额外工程、固定班长津贴、奖惩和个人固定系数生成工资内部分配表。适用于安保、消防监控、固定分段班的考勤核验、缺卡和迟到扣款、工程奖励分配和工资表制作。
---

# 安保考勤与工资分配

将原始打卡记录作为唯一考勤事实来源，先生成考勤表，再生成工资表。始终复制输入模板后再写入；不得修改原始文件、工作表名称、表头、列宽、行高、合并单元格、样式、页眉页脚或模板底部汇总区。

## 先读取规则和模板约束

在开始前读取：

- `references/confirmed-rules.md`：用户已确认的总规则、月度特殊口径和输出禁改项。
- `references/attendance-rules.md`：班组、人员、班次和待确认的规则。
- `references/payroll-rules.md`：系数工资、额外工程、班长津贴和扣款规则。
- `references/template-contract.md`：两份模板的字段映射和不得改动的区域。

将每月原始考勤、排班或休假资料、额外工程汇总和两份空白或待重算模板作为本次输入，不把这些包含人员或打卡数据的文件打包进技能。

## 强制核对关口

在生成任何工资金额前完成以下检查；任一项不通过时，只输出待核对清单，不生成最终工资表：

1. 运行 `scripts/extract_clock_records.mjs` 或等效逻辑，从原始考勤导出中识别姓名、日期和卡点。将 `59` 识别为非时间状态值，绝不解析为 00:59；不能根据文件名或缓存公式推断考勤。
2. 将模板人员、有效考勤人员、额外工程人员逐一比对，列出缺失、额外、重复和同音或近似姓名。
3. 核验当月每人的排班、带薪休假、请假、调班和岗勤同日白夜班归属。岗勤班不预设固定白班/夜班，先按同一天这一行实际卡点判班：白班为同日 `<08:00` 起始卡 + `>18:00` 结束卡；夜班为同日早上 `>08:00` 下班卡 + 当晚 `<20:00` 上班卡。为避免误用中午卡，夜班识别优先使用 `08:00—12:00` 早卡和 `18:00—20:00` 晚卡；同日有夜班早卡且晚卡不小于 `20:00` 时按夜班上班迟到。参与结算人员的月度带薪休假默认按月份计：31天的大月为6天，30天及以下的小月为5天；有明确个别请休假口径时以该口径覆盖。仅凭打卡记录无法判定整天应不应该上班时，不得把整天无卡直接算作旷工或缺卡。
4. 按已确认规则计算迟到或缺卡扣款：每次 50 元。若未来制度变更，必须由用户明确覆盖该固定值。
5. 按 `confirmed-rules.md` 使用已确认姓名口径；胡朕源班长津贴为 500 元。遇到新的同音或近似姓名时再列为待确认，不要自行合并。
6. 消防监控班当班日必须核验三类卡：起始卡 `<08:00`、中午卡 `13:00—13:30`、结束卡 `>20:00`；缺少任一必需卡按缺卡计，起始卡晚于 08:00 按迟到计。
7. 写入工资表时，若津贴、奖励、处罚或其他来源格没有可解析数字，保持对应单元格空白；不要为了占位写入 0，也不要带入模板旧公式。

## 第一步：生成考勤表

使用表格工具读取原始考勤文件和考勤模板，复制模板到输出路径。保持模板工作表名和所有格式不变。先将原始导出规范化为审计 JSON：

```text
node scripts/extract_clock_records.mjs 原始考勤.xlsx 考勤审计.json
```

按 `attendance-rules.md` 将每笔打卡归属到人员、工作日和班次：

- 岗勤班：不按固定早/夜班排班表先入为主，按同一天这一行实际卡点自动判班。白班需同日 `<08:00` 起始卡 + `>18:00` 结束卡；夜班需同日早上 `>08:00` 下班卡 + 当晚 `<20:00` 上班卡；朝洛门仅按常白班 `<08:00` + `>18:00` 核验。
- 消防监控班：按 08:00—20:00 核验，必需卡为 `<08:00` 起始卡、`13:00—13:30` 中午卡、`>20:00` 结束卡。
- 吕尚德：08:00—12:00、13:30—18:00，必需卡为 `<08:00`、`≤13:30`、`>18:00`；胡朕源：08:30—12:00、13:30—17:30，必需卡为 `≤08:30`、`≤13:30`、`>17:30`。

按已确认的排班和有效卡规则，计算实际出勤天数、缺卡次数、迟到次数、带薪休假天数及待核对事项。若某日只有一组卡且前后相邻存在连续多天无卡，优先列为休假边界或休假中返回待核对，不直接按缺卡扣款，也不自动增加出勤或休假天数。将结果写入模板既有的人员行和既有列；不要添加工作表、行或列。把逐笔打卡、班次判断和异常原因输出到独立的审计文件，而非写进严格格式的模板。

## 第二步：计算工程奖励和工资

先用考勤表中经核验的天数计算工资；不要使用模板中失效的外部链接或 `#NAME?` 结果。

计算顺序：

```text
个人总系数 = (实际出勤天数 + 带薪休假天数) × 个人固定系数
系数单价 = 系数工资总额 ÷ 全员总系数
应发系数工资 = ROUND(个人总系数 × 系数单价, 0)
额外工程应发 = ROUND(额外工程可分配金额 ÷ 工程总系数 × 个人工程总系数, 0)
迟到或缺卡扣款 = (迟到次数 + 缺卡次数) × 已确认单次扣款
工资总计 = 实发系数工资 + 带薪假 + 班长津贴 + 额外工程 + 部门奖励 + 春节加班 + 其他 - 处罚 - 迟到及缺卡
```

使用 `scripts/compute_payroll.mjs` 复核总系数、系数工资和扣款；脚本要求显式提供单次扣款，不提供默认值。额外工程先核验“工程总额－部门留存＝可分配金额”和“个人工程总系数合计”，再分配到人。

绩效池若单独给定，按 `绩效池 ÷ 全员总系数 × 个人总系数` 分配，直接并入既有“应发系数工资/实发系数工资”链路；不要写入“部门奖励”列。部门奖励列只写用户提供的部门奖励明细。

班长津贴必须按本月已确认的姓名和金额写入；不要仅凭岗位名称自动推断未确认的人员。扣款次数、处罚和工资总计都要可追溯到审计结果。

## 第三步：生成工资内部分配表

复制工资模板后，只清理和重写既有工人数据行中本月可变字段；个人固定系数列保持不变。不要删除模板中的人员行、汇总行、底部资金区或任何格式。

优先在既有计算列填入可审计公式并全行一致；若模板要求保留值，另附审计文件说明计算式。不要保留跨工作簿的失效 `VLOOKUP` 外部链接。工程奖励应写入既有“额外工程”列，班长津贴写入既有“班长津贴”列，迟到和缺卡的合并扣款写入既有“迟到及缺卡”列。

## 交付前复核

逐项核对并报告：

1. 有效考勤人员数、模板人员数、已匹配人数及待确认姓名。
2. 每人实际出勤、带薪休假、迟到、缺卡和扣款；逐笔异常附日期或时间和判定原因。
3. 全员总系数、系数工资总额、额外工程可分配金额和已分配金额、各类津贴奖惩和工资总计。
4. 工资表中是否有公式错误、外部链接错误、空白姓名匹配或汇总差额。
5. 输出文件是否仅为模板副本、两份表均通过视觉核验且未改变模板布局。

若需要先做数值预演，可运行：

```text
node scripts/compute_payroll.mjs input.json > result.json
```
